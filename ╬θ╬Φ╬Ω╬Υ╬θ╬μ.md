# Οδηγός σε απλά λόγια

## Τι είναι αυτό το project;

Φτιάχνουμε ένα πρόγραμμα που κρατάει αποτελέσματα ποδοσφαίρου (ποιος έπαιξε με ποιον και πόσο έκανε το ματς) και απαντάει σε ερωτήσεις, όπως "δώσε μου τη βαθμολογία της σεζόν" ή "δώσε μου όλα τα ματς της ομάδας Χ".

Το πρόγραμμα λέγεται **API**. Σκέψου ένα εστιατόριο:

- Η **κουζίνα** είναι η βάση δεδομένων: εκεί είναι αποθηκευμένα όλα τα ματς.
- Ο **σερβιτόρος** είναι το API: παίρνει παραγγελίες ("θέλω τη βαθμολογία"), πάει στην κουζίνα και φέρνει την απάντηση.
- Ο **πελάτης** είναι ένα άλλο πρόγραμμα, μια ιστοσελίδα ή εσύ από τον browser.

Άρα δεν φτιάχνουμε ιστοσελίδα. Φτιάχνουμε το "πίσω μέρος" (backend) που μια ιστοσελίδα ή μια εφαρμογή θα χρησιμοποιούσε.

## Γιατί το κάνουμε

Στο CV σου λείπει backend. Οι περισσότερες αγγελίες για junior ζητούν κάτι από αυτά: API, βάση δεδομένων, tests, Docker. Αυτό το project τα δείχνει όλα μαζί, σε μικρό μέγεθος.

Αργότερα θα το γεμίσεις με πραγματικά δεδομένα Bundesliga και θα το συνδέσεις με το project ανάλυσης δεδομένων σου.

## Τι κάνει το κάθε αρχείο

| Αρχείο | Τι κάνει (απλά) |
|---|---|
| `app/main.py` | Η λίστα με τις "παραγγελίες" που δέχεται το API (τα endpoints) |
| `app/models.py` | Περιγράφει τους πίνακες της βάσης: έναν για ομάδες, έναν για αγώνες |
| `app/schemas.py` | Οι κανόνες: π.χ. τα γκολ δεν μπορεί να είναι αρνητικά |
| `app/crud.py` | Η δουλειά: αποθηκεύει αγώνες και υπολογίζει τη βαθμολογία |
| `app/database.py` | Η σύνδεση με τη βάση |
| `app/importer.py` | Φορτώνει αγώνες από ένα αρχείο CSV στη βάση |
| `tests/test_api.py` | Αυτόματοι έλεγχοι ότι όλα δουλεύουν σωστά |
| `Dockerfile` | Οδηγίες για να "πακετάρεις" το πρόγραμμα ώστε να τρέχει παντού το ίδιο |
| `.github/workflows/ci.yml` | Το GitHub τρέχει μόνο του τα tests κάθε φορά που ανεβάζεις αλλαγές |
| `sample_data/sample_matches.csv` | Λίγα δοκιμαστικά ματς με φανταστικές ομάδες |

## Πώς το τρέχεις (βήμα βήμα)

Χρειάζεσαι Python. Άνοιξε το τερμινάλ μέσα στον φάκελο `football-api` και γράψε με τη σειρά:

1. `python -m venv .venv` (φτιάχνει ένα "δικό σου" περιβάλλον για το project)
2. `source .venv/bin/activate` (στα Windows: `.venv\Scripts\activate`)
3. `pip install -r requirements-dev.txt` (κατεβάζει τις βιβλιοθήκες)
4. `pytest -v` (τρέχει τους ελέγχους, πρέπει να τα δεις πράσινα)
5. `python -m app.importer sample_data/sample_matches.csv --season 2024-25` (βάζει τα δοκιμαστικά ματς στη βάση)
6. `uvicorn app.main:app --reload` (ξεκινάει το API)
7. Άνοιξε στον browser: http://127.0.0.1:8000/docs

Στη σελίδα του βήματος 7 θα δεις όλες τις "παραγγελίες" και μπορείς να τις δοκιμάσεις με κλικ. Δοκίμασε το `/standings`, βάζοντας season `2024-25`.

## Και το Docker;

Αν έχεις εγκατεστημένο το Docker, μπορείς να τρέξεις το ίδιο πρόγραμμα μέσα σε ένα "κουτί", χωρίς να εγκαταστήσεις τίποτα άλλο:

1. `docker build -t football-api .`
2. `docker run -d --name football-api -p 8000:8000 -v football-data:/app/data football-api`
3. `docker exec football-api python -m app.importer sample_data/sample_matches.csv --season 2024-25`

Μετά άνοιξε πάλι το http://127.0.0.1:8000/docs

## Σημαντικό

Ο κώδικας γράφτηκε χωρίς να τρέξουν τα tests στο περιβάλλον όπου τον έφτιαξα. Το βήμα 4 (`pytest`) είναι ο πρώτος πραγματικός έλεγχος. Αν κάτι αποτύχει, κράτα το μήνυμα λάθους και θα το διορθώσουμε μαζί.
